<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>

<body>
    <div class="container offset-2">
        <form class="mt-5" method="POST">
            <div class="form-group">
                <label for="iname">Name</label>
                <input type="text" class="form-control" id="iname" placeholder="Enter name" name="name">
            </div>
            <div class="form-group">
                <label for="iemail">Email address</label>
                <input type="email" class="form-control" id="iemail" aria-describedby="emailHelp"
                    placeholder="Enter email" name="email">
            </div>
            <div class="form-group">
                <label for="ipassword">Password</label>
                <input type="password" class="form-control" id="ipassword" placeholder="Password" name="password">
            </div>
            <button type="submit" class="btn btn-primary" formaction="/auth/signup">Submit</button>
        </form>
        <div class="mt-5">
            <a class="btn btn-warning" href="/">Back to home</a>
        </div>
        <!-- success -->
        <% if(signup){ %>
            <div class="alert alert-success mt-5" role="alert">
                <%= successMessage %>
                    <p>Please <a href="/user/signin">Sign in</a></p>
            </div>
            <% } %>
                <!-- error -->
                <% if(error){ %>
                    <div class="alert alert-danger mt-5" role="alert">
                        <%= errorMessage %>
                    </div>
                    <% } %>
    </div>
</body>

</html>